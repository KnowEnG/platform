Both types of tests use pytest as a framework.

	unit: tests that do not require nest_flask to be running 
	(but can rely on postgres and redis)

	integration: not implemented. only unit tests implemented
	here and smoke_tests implented as a separate nest_ops commmand
	are implemented
