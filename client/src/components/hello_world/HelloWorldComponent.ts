import {Component} from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'hello-world',
    templateUrl: './HelloWorldComponent.html'
})

export class HelloWorldComponent {
    class = 'relative';
}
