import {Component} from '@angular/core';
import {CanActivate} from '@angular/router';
import {tokenNotExpired} from 'angular2-jwt'

@Component({
    selector: 'individual',
    template: '<h3 style="color: #ffffff"> Cohorts will be created here using a query interface...</h3>'
})

export class CohortPlaceholder {
    
}
